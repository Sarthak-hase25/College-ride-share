<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
        h1, h2, h3 {
            font-family: 'Poppins', sans-serif;
        }
    </style>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#0F172A',
                        secondary: '#06B6D4',
                        accent: '#F59E0B',
                        dark: '#1E293B',
                        light: '#F1F5F9'
                    }
                }
            }
        }
    </script>
</head>
<body class="min-h-screen flex flex-col bg-light">
    <%- include('../partials/navbar') %>
    
    <main class="flex-grow py-12">
        <div class="container mx-auto px-4">
            <!-- Header -->
            <div class="flex flex-col md:flex-row justify-between items-center mb-8">
                <div>
                    <h1 class="text-4xl font-bold text-primary mb-2">My Offered Rides</h1>
                    <p class="text-gray-600">Manage rides you've posted</p>
                </div>
                <a href="/rides/create" class="mt-4 md:mt-0 bg-secondary text-white px-6 py-3 rounded-lg font-semibold hover:bg-cyan-500 transition-all">
                    + Offer New Ride
                </a>
            </div>

            <!-- Rides List -->
            <% if (rides && rides.length > 0) { %>
                <div class="space-y-6">
                    <% rides.forEach(ride => { %>
                        <div class="bg-white rounded-xl shadow-lg p-6 border-l-4 <%= ride.status === 'active' ? 'border-green-500' : 'border-red-500' %>">
                            <div class="flex flex-col md:flex-row justify-between gap-4">
                                <!-- Ride Info -->
                                <div class="flex-1">
                                    <div class="flex items-center gap-2 mb-4">
                                        <% if (ride.status === 'active') { %>
                                            <span class="bg-green-100 text-green-700 px-3 py-1 rounded-full text-sm font-semibold">Active</span>
                                        <% } else if (ride.status === 'cancelled') { %>
                                            <span class="bg-red-100 text-red-700 px-3 py-1 rounded-full text-sm font-semibold">Cancelled</span>
                                        <% } else { %>
                                            <span class="bg-gray-100 text-gray-700 px-3 py-1 rounded-full text-sm font-semibold">Completed</span>
                                        <% } %>
                                    </div>
                                    
                                    <!-- Route -->
                                    <div class="mb-4">
                                        <div class="flex items-center gap-3 text-lg font-bold text-gray-800">
                                            <span><%= ride.from %></span>
                                            <span class="text-secondary">→</span>
                                            <span><%= ride.to %></span>
                                        </div>
                                    </div>

                                    <!-- Details -->
                                    <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-4">
                                        <div>
                                            <p class="text-xs text-gray-500">Date</p>
                                            <p class="font-semibold">
                                                <%= new Date(ride.date).toLocaleDateString('en-IN', { day: 'numeric', month: 'short' }) %>
                                            </p>
                                        </div>
                                        <div>
                                            <p class="text-xs text-gray-500">Time</p>
                                            <p class="font-semibold"><%= ride.time %></p>
                                        </div>
                                        <div>
                                            <p class="text-xs text-gray-500">Price</p>
                                            <p class="font-semibold text-secondary">₹<%= ride.price %></p>
                                        </div>
                                        <div>
                                            <p class="text-xs text-gray-500">Booked</p>
                                            <p class="font-semibold"><%= ride.passengers.length %> / <%= ride.seats %></p>
                                        </div>
                                    </div>

                                    <!-- Passengers -->
                                    <% if (ride.passengers.length > 0) { %>
                                        <div class="mb-4">
                                            <p class="text-sm font-semibold text-gray-700 mb-2">Passengers:</p>
                                            <div class="flex flex-wrap gap-2">
                                                <% ride.passengers.forEach(passenger => { %>
                                                    <div class="bg-blue-50 border border-blue-200 px-3 py-1 rounded-lg text-sm">
                                                        <span class="font-semibold"><%= passenger.name %></span>
                                                        <span class="text-gray-600 ml-2">📱 <%= passenger.phone %></span>
                                                    </div>
                                                <% }) %>
                                            </div>
                                        </div>
                                    <% } %>
                                </div>

                                <!-- Actions -->
                                <div class="flex md:flex-col gap-2">
                                    <a href="/rides/<%= ride._id %>" class="bg-secondary text-white px-4 py-2 rounded-lg font-semibold hover:bg-cyan-500 transition-all text-center">
                                        View Details
                                    </a>
                                    <% if (ride.status === 'active') { %>
                                        <form action="/rides/<%= ride._id %>/cancel" method="POST" onsubmit="return confirm('Are you sure you want to cancel this ride?');">
                                            <button type="submit" class="w-full bg-red-500 text-white px-4 py-2 rounded-lg font-semibold hover:bg-red-600 transition-all">
                                                Cancel Ride
                                            </button>
                                        </form>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                </div>
            <% } else { %>
                <!-- No Rides -->
                <div class="text-center py-20">
                    <div class="text-6xl mb-4">🚗</div>
                    <h3 class="text-2xl font-bold text-gray-800 mb-2">No Rides Offered Yet</h3>
                    <p class="text-gray-600 mb-6">Start sharing your rides with others!</p>
                    <a href="/rides/create" class="inline-block bg-secondary text-white px-8 py-3 rounded-lg font-semibold hover:bg-cyan-500 transition-all">
                        Offer Your First Ride
                    </a>
                </div>
            <% } %>
        </div>
    </main>
    
    <%- include('../partials/footer') %>
</body>
</html>