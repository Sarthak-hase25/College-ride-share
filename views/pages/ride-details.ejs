<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
        h1, h2, h3 {
            font-family: 'Poppins', sans-serif;
        }
    </style>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#0F172A',
                        secondary: '#06B6D4',
                        accent: '#F59E0B',
                        dark: '#1E293B',
                        light: '#F1F5F9'
                    }
                }
            }
        }
    </script>
</head>
<body class="min-h-screen flex flex-col bg-light">
    <%- include('../partials/navbar') %>
    
    <main class="flex-grow py-12">
        <div class="container mx-auto px-4">
            <div class="max-w-4xl mx-auto">
                <!-- Back Button -->
                <a href="/rides" class="inline-flex items-center gap-2 text-secondary hover:text-cyan-500 mb-6 font-semibold">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                    </svg>
                    Back to Rides
                </a>

                <!-- Main Card -->
                <div class="bg-white rounded-2xl shadow-2xl overflow-hidden">
                    <!-- Header Section -->
                    <div class="bg-gradient-to-r from-primary to-dark text-white p-8">
                        <h1 class="text-3xl font-bold mb-4">Ride Details</h1>
                        
                        <!-- Route -->
                        <div class="flex items-center gap-4">
                            <div class="flex-1">
                                <p class="text-blue-200 text-sm mb-1">From</p>
                                <p class="text-2xl font-bold"><%= ride.from %></p>
                            </div>
                            <div class="text-3xl">→</div>
                            <div class="flex-1">
                                <p class="text-blue-200 text-sm mb-1">To</p>
                                <p class="text-2xl font-bold"><%= ride.to %></p>
                            </div>
                        </div>
                    </div>

                    <!-- Details Section -->
                    <div class="p-8">
                        <!-- Date & Time -->
                        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
                            <div class="bg-light p-4 rounded-xl">
                                <p class="text-gray-500 text-sm mb-1">📅 Date</p>
                                <p class="font-bold text-lg">
                                    <%= new Date(ride.date).toLocaleDateString('en-IN', { 
                                        weekday: 'long', 
                                        year: 'numeric', 
                                        month: 'long', 
                                        day: 'numeric' 
                                    }) %>
                                </p>
                            </div>
                            <div class="bg-light p-4 rounded-xl">
                                <p class="text-gray-500 text-sm mb-1">🕐 Time</p>
                                <p class="font-bold text-lg"><%= ride.time %></p>
                            </div>
                            <div class="bg-light p-4 rounded-xl">
                                <p class="text-gray-500 text-sm mb-1">💺 Seats Available</p>
                                <p class="font-bold text-lg"><%= ride.seats - ride.passengers.length %> / <%= ride.seats %></p>
                            </div>
                        </div>

                        <!-- Vehicle Details -->
                        <div class="mb-8">
                            <h2 class="text-xl font-bold text-primary mb-4">🚗 Vehicle Information</h2>
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                                <div class="border-l-4 border-secondary pl-4">
                                    <p class="text-gray-500 text-sm">Vehicle Type</p>
                                    <p class="font-semibold text-lg"><%= ride.vehicleType %></p>
                                </div>
                                <div class="border-l-4 border-secondary pl-4">
                                    <p class="text-gray-500 text-sm">Vehicle Number</p>
                                    <p class="font-semibold text-lg"><%= ride.vehicleNumber %></p>
                                </div>
                            </div>
                        </div>

                        <!-- Driver Details -->
                        <div class="mb-8">
                            <h2 class="text-xl font-bold text-primary mb-4">👤 Driver Information</h2>
                            <div class="bg-light p-6 rounded-xl">
                                <div class="flex items-start gap-4">
                                    <div class="w-16 h-16 bg-gradient-to-br from-secondary to-cyan-400 rounded-full flex items-center justify-center text-2xl text-white font-bold">
                                        <%= ride.driver.name.charAt(0).toUpperCase() %>
                                    </div>
                                    <div class="flex-1">
                                        <p class="font-bold text-lg text-gray-800"><%= ride.driver.name %></p>
                                        <p class="text-gray-600"><%= ride.driver.college %></p>
                                        <p class="text-gray-600">📧 <%= ride.driver.email %></p>
                                        <p class="text-gray-600">📱 <%= ride.driver.phone %></p>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Additional Notes -->
                        <% if (ride.notes) { %>
                            <div class="mb-8">
                                <h2 class="text-xl font-bold text-primary mb-4">📝 Additional Notes</h2>
                                <div class="bg-amber-50 border-l-4 border-accent p-4 rounded">
                                    <p class="text-gray-700"><%= ride.notes %></p>
                                </div>
                            </div>
                        <% } %>

                        <!-- Passengers -->
                        <% if (ride.passengers.length > 0) { %>
                            <div class="mb-8">
                                <h2 class="text-xl font-bold text-primary mb-4">👥 Passengers (<%= ride.passengers.length %>)</h2>
                                <div class="flex flex-wrap gap-3">
                                    <% ride.passengers.forEach(passenger => { %>
                                        <div class="bg-light px-4 py-2 rounded-full">
                                            <span class="font-semibold"><%= passenger.name %></span>
                                        </div>
                                    <% }) %>
                                </div>
                            </div>
                        <% } %>

                        <!-- Price & Booking -->
                        <div class="border-t-2 border-gray-200 pt-6">
                            <div class="flex flex-col md:flex-row items-center justify-between gap-4">
                                <div>
                                    <p class="text-gray-500 text-sm">Total Price</p>
                                    <p class="text-4xl font-bold text-secondary">₹<%= ride.price %></p>
                                    <p class="text-sm text-gray-500">per seat</p>
                                </div>
                                
                                <% if (user && ride.driver._id.toString() === user.id) { %>
                                    <!-- Driver's own ride -->
                                    <div class="bg-green-50 border border-green-200 text-green-700 px-6 py-3 rounded-lg">
                                        <p class="font-semibold">✅ This is your ride</p>
                                    </div>
                                <% } else if (user && ride.passengers.some(p => p._id.toString() === user.id)) { %>
                                    <!-- Already booked -->
                                    <div class="bg-blue-50 border border-blue-200 text-blue-700 px-6 py-3 rounded-lg">
                                        <p class="font-semibold">✅ You've booked this ride</p>
                                    </div>
                                <% } else if (ride.seats - ride.passengers.length > 0) { %>
                                    <!-- Available to book -->
                                    <form action="/rides/<%= ride._id %>/book" method="POST">
                                        <button 
                                            type="submit"
                                            class="bg-secondary text-white px-8 py-4 rounded-lg font-bold text-lg hover:bg-cyan-500 transition-all hover:scale-105 shadow-lg"
                                        >
                                            Book This Ride
                                        </button>
                                    </form>
                                <% } else { %>
                                    <!-- Fully booked -->
                                    <div class="bg-red-50 border border-red-200 text-red-700 px-6 py-3 rounded-lg">
                                        <p class="font-semibold">❌ Fully Booked</p>
                                    </div>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>
    
    <%- include('../partials/footer') %>
</body>
</html>