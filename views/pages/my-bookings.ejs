<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
        h1, h2, h3 {
            font-family: 'Poppins', sans-serif;
        }
    </style>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#0F172A',
                        secondary: '#06B6D4',
                        accent: '#F59E0B',
                        dark: '#1E293B',
                        light: '#F1F5F9'
                    }
                }
            }
        }
    </script>
</head>
<body class="min-h-screen flex flex-col bg-light">
    <%- include('../partials/navbar') %>
    
    <main class="flex-grow py-12">
        <div class="container mx-auto px-4">
            <!-- Header -->
            <div class="flex flex-col md:flex-row justify-between items-center mb-8">
                <div>
                    <h1 class="text-4xl font-bold text-primary mb-2">My Bookings</h1>
                    <p class="text-gray-600">Rides you've booked</p>
                </div>
                <a href="/rides" class="mt-4 md:mt-0 bg-secondary text-white px-6 py-3 rounded-lg font-semibold hover:bg-cyan-500 transition-all">
                    Find More Rides
                </a>
            </div>

            <!-- Bookings List -->
            <% if (rides && rides.length > 0) { %>
                <div class="space-y-6">
                    <% rides.forEach(ride => { %>
                        <div class="bg-white rounded-xl shadow-lg p-6 border-l-4 border-blue-500">
                            <div class="flex flex-col md:flex-row justify-between gap-4">
                                <!-- Ride Info -->
                                <div class="flex-1">
                                    <!-- Route -->
                                    <div class="mb-4">
                                        <div class="flex items-center gap-3 text-lg font-bold text-gray-800">
                                            <span><%= ride.from %></span>
                                            <span class="text-secondary">→</span>
                                            <span><%= ride.to %></span>
                                        </div>
                                    </div>

                                    <!-- Details -->
                                    <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-4">
                                        <div>
                                            <p class="text-xs text-gray-500">Date</p>
                                            <p class="font-semibold">
                                                <%= new Date(ride.date).toLocaleDateString('en-IN', { day: 'numeric', month: 'short', year: 'numeric' }) %>
                                            </p>
                                        </div>
                                        <div>
                                            <p class="text-xs text-gray-500">Time</p>
                                            <p class="font-semibold"><%= ride.time %></p>
                                        </div>
                                        <div>
                                            <p class="text-xs text-gray-500">Price</p>
                                            <p class="font-semibold text-secondary">₹<%= ride.price %></p>
                                        </div>
                                        <div>
                                            <p class="text-xs text-gray-500">Vehicle</p>
                                            <p class="font-semibold"><%= ride.vehicleType %></p>
                                        </div>
                                    </div>

                                    <!-- Driver Info -->
                                    <div class="bg-gray-50 p-4 rounded-lg">
                                        <p class="text-sm font-semibold text-gray-700 mb-2">Driver:</p>
                                        <div class="flex items-center gap-3">
                                            <div class="w-10 h-10 bg-gradient-to-br from-secondary to-cyan-400 rounded-full flex items-center justify-center text-white font-bold">
                                                <%= ride.driver.name.charAt(0).toUpperCase() %>
                                            </div>
                                            <div>
                                                <p class="font-semibold"><%= ride.driver.name %></p>
                                                <p class="text-sm text-gray-600">📱 <%= ride.driver.phone %></p>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <!-- Actions -->
                                <div class="flex md:flex-col gap-2">
                                    <a href="/rides/<%= ride._id %>" class="bg-secondary text-white px-4 py-2 rounded-lg font-semibold hover:bg-cyan-500 transition-all text-center">
                                        View Details
                                    </a>
                                    <form action="/rides/<%= ride._id %>/cancel-booking" method="POST" onsubmit="return confirm('Are you sure you want to cancel this booking?');">
                                        <button type="submit" class="w-full bg-red-500 text-white px-4 py-2 rounded-lg font-semibold hover:bg-red-600 transition-all">
                                            Cancel Booking
                                        </button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                </div>
            <% } else { %>
                <!-- No Bookings -->
                <div class="text-center py-20">
                    <div class="text-6xl mb-4">📅</div>
                    <h3 class="text-2xl font-bold text-gray-800 mb-2">No Bookings Yet</h3>
                    <p class="text-gray-600 mb-6">Start booking rides and save money!</p>
                    <a href="/rides" class="inline-block bg-secondary text-white px-8 py-3 rounded-lg font-semibold hover:bg-cyan-500 transition-all">
                        Find a Ride
                    </a>
                </div>
            <% } %>
        </div>
    </main>
    
    <%- include('../partials/footer') %>
</body>
</html>