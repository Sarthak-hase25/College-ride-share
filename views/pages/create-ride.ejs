<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
        h1, h2, h3 {
            font-family: 'Poppins', sans-serif;
        }
    </style>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#0F172A',
                        secondary: '#06B6D4',
                        accent: '#F59E0B',
                        dark: '#1E293B',
                        light: '#F1F5F9'
                    }
                }
            }
        }
    </script>
</head>
<body class="min-h-screen flex flex-col bg-light">
    <%- include('../partials/navbar') %>
    
    <main class="flex-grow py-12 px-4">
        <div class="max-w-2xl mx-auto">
            <!-- Header -->
            <div class="text-center mb-8">
                <h1 class="text-4xl font-bold text-primary mb-2">Offer a Ride</h1>
                <p class="text-gray-600">Share your journey and help others save money!</p>
            </div>

            <!-- Form Card -->
            <div class="bg-white rounded-2xl shadow-2xl p-8">
                <!-- Error Message -->
                <% if (error) { %>
                    <div class="mb-6 bg-red-50 border-l-4 border-red-500 p-4 rounded">
                        <p class="text-red-700 text-sm"><%= error %></p>
                    </div>
                <% } %>

                <form action="/rides/create" method="POST">
                    <!-- Route Section -->
                    <div class="mb-6">
                        <h2 class="text-xl font-bold text-primary mb-4 flex items-center gap-2">
                            <span>📍</span> Route Details
                        </h2>
                        
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <!-- From -->
                            <div>
                                <label class="block text-gray-700 text-sm font-semibold mb-2" for="from">
                                    From (Pickup Location)
                                </label>
                                <input 
                                    class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-secondary focus:border-transparent transition-all" 
                                    id="from" 
                                    name="from" 
                                    type="text"
                                    required
                                >
                            </div>

                            <!-- To -->
                            <div>
                                <label class="block text-gray-700 text-sm font-semibold mb-2" for="to">
                                    To (Destination)
                                </label>
                                <input 
                                    class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-secondary focus:border-transparent transition-all" 
                                    id="to" 
                                    name="to" 
                                    type="text"
                                    required
                                >
                            </div>
                        </div>
                    </div>

                    <!-- Date & Time Section -->
                    <div class="mb-6">
                        <h2 class="text-xl font-bold text-primary mb-4 flex items-center gap-2">
                            <span>🕐</span> When?
                        </h2>
                        
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <!-- Date -->
                            <div>
                                <label class="block text-gray-700 text-sm font-semibold mb-2" for="date">
                                    Date
                                </label>
                                <input 
                                    class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-secondary focus:border-transparent transition-all" 
                                    id="date" 
                                    name="date" 
                                    type="date"
                                    required
                                >
                            </div>

                            <!-- Time -->
                            <div>
                                <label class="block text-gray-700 text-sm font-semibold mb-2" for="time">
                                    Time
                                </label>
                                <input 
                                    class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-secondary focus:border-transparent transition-all" 
                                    id="time" 
                                    name="time" 
                                    type="time"
                                    required
                                >
                            </div>
                        </div>
                    </div>

                    <!-- Vehicle & Pricing Section -->
                    <div class="mb-6">
                        <h2 class="text-xl font-bold text-primary mb-4 flex items-center gap-2">
                            <span>🚗</span> Vehicle & Pricing
                        </h2>
                        
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-4">
                            <!-- Vehicle Type -->
                            <div>
                                <label class="block text-gray-700 text-sm font-semibold mb-2" for="vehicleType">
                                    Vehicle Type
                                </label>
                                <select 
                                    class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-secondary focus:border-transparent transition-all" 
                                    id="vehicleType" 
                                    name="vehicleType"
                                    required
                                >
                                    <option value="">Select vehicle type</option>
                                    <option value="Car">Car</option>
                                    <option value="Bike">Bike</option>
                                    <option value="Auto">Auto</option>
                                    <option value="SUV">SUV</option>
                                </select>
                            </div>

                            <!-- Vehicle Number -->
                            <div>
                                <label class="block text-gray-700 text-sm font-semibold mb-2" for="vehicleNumber">
                                    Vehicle Number
                                </label>
                                <input 
                                    class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-secondary focus:border-transparent transition-all uppercase" 
                                    id="vehicleNumber" 
                                    name="vehicleNumber" 
                                    type="text"
                                    required
                                >
                            </div>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <!-- Seats -->
                            <div>
                                <label class="block text-gray-700 text-sm font-semibold mb-2" for="seats">
                                    Available Seats
                                </label>
                                <input 
                                    class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-secondary focus:border-transparent transition-all" 
                                    id="seats" 
                                    name="seats" 
                                    type="number"
                                    min="1"
                                    max="6"
                                    required
                                >
                            </div>

                            <!-- Price -->
                            <div>
                                <label class="block text-gray-700 text-sm font-semibold mb-2" for="price">
                                    Price per Seat (₹)
                                </label>
                                <input 
                                    class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-secondary focus:border-transparent transition-all" 
                                    id="price" 
                                    name="price" 
                                    type="number"
                                    min="0"
                                    required
                                >
                            </div>
                        </div>
                    </div>

                    <!-- Additional Notes -->
                    <div class="mb-6">
                        <label class="block text-gray-700 text-sm font-semibold mb-2" for="notes">
                            Additional Notes (Optional)
                        </label>
                        <textarea 
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-secondary focus:border-transparent transition-all" 
                            id="notes" 
                            name="notes"
                            rows="3"
                            maxlength="500"
                        ></textarea>
                        <p class="text-xs text-gray-500 mt-1">Max 500 characters</p>
                    </div>

                    <!-- Submit Button -->
                    <div class="flex gap-4">
                        <button 
                            type="submit"
                            class="flex-1 bg-secondary text-white font-semibold py-3 px-6 rounded-lg hover:bg-cyan-500 transition-all hover:scale-105 shadow-lg"
                        >
                            Post Ride
                        </button>
                        <a 
                            href="/dashboard"
                            class="flex-1 bg-gray-200 text-gray-700 font-semibold py-3 px-6 rounded-lg hover:bg-gray-300 transition-all text-center"
                        >
                            Cancel
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </main>
    
    <%- include('../partials/footer') %>

    <script>
        // Set minimum date to today
        document.getElementById('date').min = new Date().toISOString().split('T')[0];
    </script>
</body>
</html>